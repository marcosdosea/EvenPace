@model IEnumerable<Models.HistoricoEventoViewModel>

<link rel="stylesheet" href="~/css/historico-eventos.css" />

<h4 class="mb-3 text-center">📅 Histórico de Eventos</h4>

<div class="d-flex flex-column gap-3">
@foreach (var item in Model)
{
    <div class="card shadow-sm">
        <div class="card-body">
            <h5 class="card-title">@item.Nome</h5>

            <p class="mb-1">
                <strong>📆 Data:</strong> @item.Data.ToShortDateString()
            </p>

            <p class="mb-0">
                <strong>📍 Local:</strong> @item.Local
            </p>
        </div>
    </div>
}
</div>
