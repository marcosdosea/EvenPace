@model EvenPaceWeb.Models.TelaInscricaoViewModel
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cancelar Inscrição</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f5c400;
            margin: 0;
            padding: 20px;
        }

        /*.phone {
            max-width: 900px;
            margin: 30px auto;
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 4px 12px rgba(0,0,0,.08);
            display: flex;
            flex-direction: column;
            min-height: 90vh;
            overflow: hidden;
        }*/



        .phone {
    width: 360px;              
    max-width: 100%;          
    height: 640px;             
    margin: 20px auto;
    background-color: #fff;
    border-radius: 30px;       
    box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    font-size: 16px;


    border: 1px solid #ccc;   
    position: relative;
}


        .header {
            padding: 14px 18px;
            font-size: 18px;
            cursor: pointer;
            border-bottom: 1px solid #eee;
        }

        .app-content {
            flex: 1;
            padding: 25px;
            overflow-y: auto;
        }

        h2 {
            font-weight: 700;
            text-align: center;
            margin-bottom: 25px;
        }

        .card-box {
            background: #fff;
            border-radius: 12px;
            padding: 18px;
            box-shadow: 0 2px 8px rgba(0,0,0,.08);
            margin-bottom: 20px;
        }

        .card-box.top {
            border-top: 6px solid #FFD700;
        }

        .label-pequeno {
            font-size: .85rem;
            color: #6c757d;
        }

        .valor-negrito {
            font-weight: bold;
            margin: 0;
        }

        .alerta {
            background: #fff3cd;
            border-radius: 12px;
            padding: 18px;
            font-size: .95rem;
        }

        .alerta ul {
            padding-left: 18px;
            margin: 10px 0 0;
        }

        .botoes {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }

        .btn-voltar {
            flex: 1;
            border: 2px solid #000;
            background: #fff;
            color: #000;
            border-radius: 30px;
            font-weight: bold;
            padding: 14px;
        }

        .btn-cancelar {
            flex: 1;
            background: #FFD700;
            border: none;
            border-radius: 30px;
            font-weight: bold;
            padding: 14px;
        }

        .btn-cancelar:hover {
            background: #f1c40f;
        }
    </style>
</head>

<body>

<div class="phone">

    <div class="header" onclick="history.back()">← Voltar</div>

    <form asp-controller="Inscricao" asp-action="Cancelar" method="post" class="app-content">
            @Html.AntiForgeryToken()

   
        <input type="hidden" asp-for="Inscricao.Id" />
        <input type="hidden" asp-for="Inscricao.IdEvento" />

        <h2>Cancelar inscrição ⚠️</h2>

        <div class="card-box top">
            <p class="valor-negrito">@Model.NomeEvento</p>
            <span class="label-pequeno">Evento</span>

            <hr />

            <p class="mb-1"><strong>📅 Data:</strong> @Model.DataEvento.ToString("dd/MM/yyyy")</p>
            <p class="mb-1"><strong>📍 Local:</strong> @Model.Local</p>
            <p class="mb-0"><strong>🏃 Distância:</strong> @Model.Inscricao.Distancia</p>
        </div>

        <div class="card-box">
            <p class="valor-negrito">Sua inscrição</p>
            <span class="label-pequeno">Detalhes</span>

            <hr />

            <p class="mb-1">
                <strong>Corredor:</strong> @Model.NomeCorredor
            </p>
            <p class="mb-1">
                <strong>Data da inscrição:</strong> @Model.Inscricao.DataInscricao.ToString("dd/MM/yyyy")
            </p>
            <p class="mb-0">
                <strong>Kit escolhido:</strong> @Model.NomeKit
            </p>
        </div>

        <div class="alerta">
            <strong>⚠️ Ao cancelar sua inscrição:</strong>
            <ul>
                <li>Você perderá a vaga na corrida</li>
                <li>Esta ação não poderá ser desfeita</li>

            </ul>
        </div>

        <div class="botoes">


            <button type="submit" class="btn-cancelar">
                🗑️ Cancelar inscrição
            </button>
        </div>

    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

